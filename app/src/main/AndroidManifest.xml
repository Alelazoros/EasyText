<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.antonchuraev.vkmessenger">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".Authorization.Retrofit.RetrofitApplication"
            android:allowBackup="true"
            android:icon="@mipmap/logo"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/logo_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".DisplayMessages.FullDialog.FullDialog">
        </activity>
        <activity android:name=".DisplayMessages.Adapter.MyRecyclerAllDialogsAdapter">
        </activity>
        <activity android:name=".Authorization.Authorization">
        </activity>
        <activity android:name=".DisplayMessages.AllDialogs">
        </activity>
        <activity android:name=".Authorization.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>